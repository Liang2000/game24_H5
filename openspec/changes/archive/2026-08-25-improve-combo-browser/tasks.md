## 1. 牌面卡片两行布局

- [x] 1.1 将 `ComboScene` 的卡片网格改为 7 列 × 2 行：第 1 行 7 张牌、第 2 行 6 张牌，第二行末位放【清空】按钮（复用 `addButton`）。验证：浏览器中卡片按 7+6 两行排列，第二行末位显示【清空】。
- [x] 1.2 将 `resultHeader` 下移到第二行卡片下方，确保「数据已就绪，请选择一张牌」等提示不被 J/Q/K 卡片遮挡。验证：浏览器中提示文字与卡片无重叠。

## 2. 多选并列过滤

- [x] 2.1 引入 `selected: number[]` 选择状态：点未选卡片且已选 <4 则加入；再点已选卡片则移出（toggle）；已选 4 张时点第 5 张忽略。验证：点 3、4 后 `selected=[3,4]`，再点 3 变 `[4]`，选满 4 张后点第 5 张不变。
- [x] 2.2 选中卡片以淡绿色底色标记，未选卡片恢复默认 `COLORS.card` 底色。验证：选中 3、4 时两卡底色淡绿，其余默认。
- [x] 2.3 过滤改为 AND 关系：`selected.every(v => combo.includes(v))`；【清空】清空 `selected` 并重置结果与标题。验证：点 3、4 只显示同时含 3 与 4 的组合；点【清空】回到「请选择」初始态。

## 3. 结果三栏展示

- [x] 3.1 结果改为三栏渲染：第 `i` 项放 `col = i % 3`、`row = floor(i / 3)`，列宽 = `viewportW / 3`，首栏左缘留 ≥ 一个 `gapX` 边距；`maxScroll` 按 `ceil(count / 3) * lineH` 计算。验证：点 Q（380 条）时结果三栏排布、首栏数字完整不越界、可滚动到底部。

## 4. 回归验证

- [x] 4.1 运行 `npm test` 与 `npm run build`。验证：现有测试全部通过、构建无 TS 错误，单人挑战与提示两模块行为不变。
