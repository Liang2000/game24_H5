# 单元测试报告

- 生成时间：2026-08-25 12:14:43
- 测试框架：Vitest 4.1.11
- 运行命令：`npm test`（=`vitest run`）

## 总览

| 指标 | 结果 |
|---|---|
| 测试文件 | 3 个（全部通过） |
| 测试用例 | 15 个（全部通过） |
| 执行耗时 | 227ms |

## 按模块明细

### `src/core/game.test.ts` — 领域逻辑（5 个）

| 用例 | 覆盖点 |
|---|---|
| `deck > has 52 cards, 4 of each value` | 标准 52 张牌组，每个牌面数值 1–13 各 4 张 |
| `deck > draws 4 valid cards` | 抽题返回 4 张，数值均落在 1–13 |
| `faceLabel > maps A/J/Q/K and plain numbers` | 牌面映射 A/2–10/J/Q/K |
| `scoring > applies +1 / 0 / -1` | 计分：对 +1、跳过 0、错 −1 |
| `scoring > passes at score >= 8` | 过关阈值 ≥ 8 |

### `src/core/solver.test.ts` — 求解器（5 个）

| 用例 | 覆盖点 |
|---|---|
| `finds all essentially-different solutions for 3,8,8,9` | 枚举 7 条本质不同解（README 的 3 条为示例子集） |
| `returns solutions in ascending order` | 输出升序 |
| `finds the trivial 1*2*3*4 solution` | 平凡解包含性 |
| `judges 1,1,1,1 unsolvable` | `isSolvable` 无解判定 |
| `caps output at 20 solutions` | 输出上限 ≤ 20 |

### `src/core/expression.test.ts` — 表达式校验（5 个）

| 用例 | 覆盖点 |
|---|---|
| `accepts a correct expression` | 合法算式 `3*8*(9-8)` |
| `accepts full-width operators and parens` | 全角 `×÷（）−` 归一化 |
| `rejects using the wrong numbers` | 用了给定之外的数字 → 拒绝 |
| `rejects division by zero` | 除 0 拦截 |
| `rejects a wrong result` | 结果 ≠ 24 → 拒绝 |

## 覆盖说明

- **直接覆盖**：牌组/抽题/计分/过关、求解器枚举与无解判定、表达式校验。
- **间接覆盖**（无独立用例）：`rational.ts`（精确有理数）与 `expr.ts`（表达式树/规范化打印）经 `solver` 与 `expression` 的用例间接验证——例如「7 条本质不同解」即校验了交换律/结合律合并与去重逻辑。
- **未覆盖**：三个 Phaser 场景（`MenuScene`/`ChallengeScene`/`HintScene`）的 UI 交互未写单测——Phaser 画布逻辑不适合单测，已改用浏览器运行时验证（含两处 bug 的场景状态硬断言）。
- **缺口**：`solver.test.ts` 的「≤20 上限」目前只验证了 ≤20，未用真正能产生 20 条以上的题目断言「恰好 20」的截断行为；可补一个高解数题目（如部分多因子组合）强化该边界。
